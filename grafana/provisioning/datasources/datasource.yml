apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090

  - name: MySQL
    type: mysql
    url: mysql:3306
    database: energy
    user: root
    secureJsonData:
      password: root